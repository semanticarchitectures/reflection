/**
 * Context Generation System
 *
 * A CLI/agent-driven tool that accepts a topic and use case,
 * refines the request through interactive clarification questions,
 * and produces a structured set of markdown context files.
 */

export {};
