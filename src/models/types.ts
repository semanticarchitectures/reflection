/**
 * Constants defining system limits for the Context Generation system.
 */

/** Maximum length for a topic description in characters. */
export const MAX_TOPIC_LENGTH = 2000;

/** Maximum length for a use case description in characters. */
export const MAX_USE_CASE_LENGTH = 1000;

/** Minimum number of non-whitespace characters required for valid input. */
export const MIN_CONTENT_LENGTH = 10;

/** Maximum number of context files in a context set. */
export const MAX_FILES = 10;

/** Minimum number of context files in a context set. */
export const MIN_FILES = 2;

/** Maximum filename length including the .md extension. */
export const MAX_FILENAME_LENGTH = 60;
